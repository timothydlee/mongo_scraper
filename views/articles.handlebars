<div class="jumbotron">
	<h1>All CinemaBlend Articles</h1>
	<p>Click on an article to read it, save it to read later, or add a comment.</p>
</div>

{{#each articles}}
	<div class="input-group articles-list">
		<a href="{{this.link}}" target="_blank" class="form-control">{{this.title}}</a>
		<span class="input-group-btn">		
			<form action="/saved/{{this.id}}" method="POST">
				<button type="submit" class="btn btn-secondary save">Save Article</button>
			</form>
		</span>
		<span class="input-group-btn">
			<form action='/articles/{{this.id}}' method="GET">
				<button type="submit" class="btn btn-secondary">Note</button>
			</form>
		</span>
	</div>
	<br>
{{/each}}
